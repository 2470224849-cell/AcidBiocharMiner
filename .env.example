# Copy to .env only if your own launcher loads dotenv files.
# The bundled shell entry point reads DEEPSEEK_API_KEY from the process environment.
DEEPSEEK_API_KEY=replace_with_your_own_key
AIHUBMIX_API_KEY=replace_with_your_own_key

